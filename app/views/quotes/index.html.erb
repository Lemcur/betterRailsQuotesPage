
<div class="wrapper">
  <h1>Top quality quotes!</h1>
  <p class="alert"><%= alert %></p>
  <p class="notice"><%= notice %></p>
  <% @quotes.each do |quote| %>
    <div class="citation-container">
      <div class="quote-div">
        <q> <%= quote.content %></q>
        <cite><%= quote.author %></cite>
      </div>
      <div class="operations-div">
        <div class="operations-on-quotes">
          <%= link_to image_tag("pencil-edit-button.png"), edit_quote_path(quote) %>
          <%= link_to image_tag("trash-can.png"), quote, method: :delete, data: { confirm: 'Are you sure?' } %>
        </div>
        <time><%= quote.created_at.strftime("%d %B %Y") %></time>
      </div>
    </div>
  <% end %>
  <div class="new">
      <%= link_to 'New quote', new_quote_path %>
    </div>
  <%= will_paginate @quotes, previous_label: h("< "), next_label: h("> ") %>
  </div>
